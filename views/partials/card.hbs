<div class="stat-card {{color}}" data-type="{{type}}">
    <h3 class="stat-card__title"><i class="material-icons">{{icon}}</i> {{key}}</h3>
    <span class="stat-card__value">{{value}} {{suffix}}</span>
</div>